<h2>UPload a CSV file to import into the databse</h2>

<% form_for @import, :html => { :multipart => true } do |f| %>

	<%= f.file_field :csv %>
	<select name="import[datatype]" size="1">
		<option value="releases">Picture Info</option>
	</select>
	
	<%= f.submit "Import" %>
<% end %>